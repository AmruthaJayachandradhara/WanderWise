"""Budget node — stub placeholder.

Full implementation added in a later commit when FX normalisation
and LLM-based offer selection are wired in.
"""

from backend.app.orchestrator.state import GraphState


def budget_node(state: GraphState) -> dict:
    return {}
